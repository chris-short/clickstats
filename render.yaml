services:
  - type: web
    name: clickstats
    env: docker
    plan: free
    healthCheckPath: /api/config
    envVars:
      - key: BUTTONDOWN_API_KEY
        sync: false
      - key: CLICKSTATS_NAME
        sync: false
